<?php if($tasks->count() == 0): ?>
  
   <h1>No tasks</h1>
      
<?php else: ?>
    
     <div class="board-header">
      <div class="board-header-section">
         <h1>Tasks</h1>
      </div>
      <div class="board-header-section">
         <h1>To do</h1>
      </div>
      <div class="board-header-section">
         <h1>Doing</h1>
      </div>
      <div class="board-header-section">
         <h1>Done</h1>
      </div>
   </div>

   <?php foreach ($tasks as $task): ?>
   <div class="board" task-id="<?php echo $task->id; ?>">
      <div class="board-first-column" section="board-task">
        <h4><?php echo $task->name; ?></h4>
      </div>
      <div class="board-section" id="todo" section="TODO">
         <?php
            
            if($task->state == "TODO")
            {
               foreach($membersForTask[$task->id] as $member)
               {
                  echo '<div id="'.$member->email.$task->id.'" member-id="'.$member->id.'" class="board-user"><img draggable="false" class="imgProject" width="40px" src="'.$this->basePath('img/users/'.$member->filePhoto).'"><br>'.$member->firstName.'</div>';
               }

            }
         ?>
      </div>
      <div class="board-section" id="doing" section="DOING">
         <?php
            if($task->state == "DOING")
            {
               foreach($membersForTask[$task->id] as $member)
               {
                  echo '<div id="'.$member->email.$task->id.'" member-id="'.$member->id.'" class="board-user"><img draggable="false" class="imgProject" width="40px" src="'.$this->basePath('img/users/'.$member->filePhoto).'"><br>'.$member->firstName.'</div>';
               }
            }
         ?>
      </div>
      <div class="board-section" id="done" section="DONE">
         <?php
            if($task->state == "DONE")
            {
               foreach($membersForTask[$task->id] as $member)
               {
                  echo '<div id="'.$member->email.$task->id.'" member-id="'.$member->id.'" class="board-user"><img draggable="false" class="imgProject" width="40px" src="'.$this->basePath('img/users/'.$member->filePhoto).'"><br>'.$member->firstName.'</div>';
               }
            }
         ?>
      </div>
   </div>
   <?php endforeach; ?>

<?php endif; ?>
